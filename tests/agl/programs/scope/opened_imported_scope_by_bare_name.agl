# A glob import contributes an imported scope's members bare; a later use of
# that scope's own name (with no module route) reaches them too.
import facade::*

use Shapes::*

program def main() -> unit =
  print(measure())
  print(Nested::Point(x = 4).x)
