# for x in array do body — iterate array elements, accumulate sum.
# Exercises: for over array, for var in body, loop-carried var, done terminator.
param items: array[int]
var total: int = 0
for x in items do
  total := total + x
done
print total
