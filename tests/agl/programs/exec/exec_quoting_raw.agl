# Shell-safe interpolation quotes each value; `as raw` deliberately bypasses
# quoting.
input arg
input k: int

let quoted = exec "printf '%s|' ${arg}"
print quoted

let raw_split = exec "printf '%s|' ${arg as raw}"
print raw_split

let digits = exec "printf '%s' ${k}"
print digits
