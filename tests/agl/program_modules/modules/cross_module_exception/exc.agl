# Library module defining exceptions consumed cross-module.
# Unmarked fields use the standard positional-or-named default.
exception Boom extends Exception
  code: int

# Exception with two own fields — proves kinds list length correct cross-module.
exception BoomDetail extends Exception
  code: int
  detail: text
