# Library module completing the cross-module cycle with tree_a: NodeB holds
# a list[NodeA] (defined in tree_a), which in turn can hold a NodeB again via
# BranchA. The list guards the recursion, exactly like a same-module
# list-guarded record would.
open import tree_a
record NodeB
  tag: text
  children: list[NodeA]
