# An unannotated exported var with an empty array literal initializer is
# still rejected for needing an annotation, whether or not it is imported.
import store

program def main() -> unit =
  print store::xs
