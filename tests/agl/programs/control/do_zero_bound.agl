# A non-positive bound runs the body zero times and completes normally (no
# MaxIterationsExceeded), even though a positive-bound loop is post-test and
# always runs at least once.

program def main(k: int) -> unit =
  var rounds: int = 0

  do[k]
    rounds := rounds + 1
  until rounds >= 1
  print rounds
