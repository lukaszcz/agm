# Recursive enum: Tree is guarded by its Leaf base-case variant (Node always
# carries two more Trees, but Leaf needs nothing). Builds a small tree from
# params, sums and measures its depth via recursive functions, pattern-matches
# it exhaustively, and compares two structurally (in)equal trees with '='.
enum Tree
  | Leaf
  | Node(value: int, left: Tree, right: Tree)

def sum-tree(t: Tree) -> int =
  case t of
    | Leaf() => 0
    | Node(value, left, right) => value + sum-tree(left) + sum-tree(right)

def tree-depth(t: Tree) -> int =
  case t of
    | Leaf() => 0
    | Node(value, left, right) =>
        let l = tree-depth(left)
        let r = tree-depth(right)
        1 + (if l >= r => l else => r)

program def main(a: int, b: int, c: int) -> unit =
  let tree: Tree = Node(
    value = a,
    left = Node(value = b, left = Leaf(), right = Leaf()),
    right = Node(value = c, left = Leaf(), right = Leaf()),
  )
  let same-shape: Tree = Node(
    value = a,
    left = Node(value = b, left = Leaf(), right = Leaf()),
    right = Node(value = c, left = Leaf(), right = Leaf()),
  )
  let different: Tree = Node(value = a, left = Leaf(), right = Leaf())

  print sum-tree(tree)
  print tree-depth(tree)
  print (tree == same-shape)
  print (tree == different)

  case tree of
    | Leaf() => print "empty"
    | Node(value, left, right) => print "root %{value}"
