enum NonEmpty[T]
  | Last(value: T)
  | More(value: T, rest: NonEmpty[T])

def first[T](items: NonEmpty[T]) =
  case items of
    | Last(value) => value
    | More(value, rest) => first(rest)

program def main(first-value: int, next-value: int) -> unit =
  let items: NonEmpty[int] = More(value = first-value, rest = Last(value = next-value))
  print first(items)
