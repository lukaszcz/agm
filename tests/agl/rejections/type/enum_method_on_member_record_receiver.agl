enum Tree[T]
  | Leaf
  | Node(value: T)

def Tree::describe[T](self) -> text =
  case self of
    | Leaf => "leaf"
    | Node(value) => "node %{value}"

program def main() -> unit =
  let node = Node(value = 1)
  print(node.describe())
