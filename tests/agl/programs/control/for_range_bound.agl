# Range for + do[n] safety bound: MaxIterationsExceeded raised after n body entries.
# Exercises: composition of range for with a do[n] safety bound.
param bound: int

for i in 1 to 1000 do[bound]
  ()
done
print "unreachable"
