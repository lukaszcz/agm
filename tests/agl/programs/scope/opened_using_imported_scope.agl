# A brace-tail import contributes scope A's members bare; a later scope use
# (with no module route) reaches the source scope those came from.
import facade::{Shapes}

use Shapes::*

program def main() -> unit =
  print(measure())
  print(Nested::Point(x = 5).x)
