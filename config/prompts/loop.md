Read ${TASKS_DIR}/PROGRESS.md and ${TASKS_DIR}/TASK_INDEX.md. Choose a task that has not yet been completed and is not blocked by other tasks. Read the corresponding task file and implement the task. Once you're finished implementing the task, commit, update ${TASKS_DIR}/PROGRESS.md and other relevant files in ${TASKS_DIR}, commit in ${TASKS_DIR} (separate repository, gitignored in the parent repo), and stop.

CRITICAL: Before you stop, always update ${TASKS_DIR/PROGRESS.md and commit in ${TASKS_DIR}

CRITICAL: If you are CERTAIN that ALL tasks are complete, reply COMPLETE on a single line (no other text)
