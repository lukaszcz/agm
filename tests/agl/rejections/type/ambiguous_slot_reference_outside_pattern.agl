enum Color
  | Red
  | Blue
enum Signal
  | Red
  | Green
enum Wrap
  | wrap(shade: Color)
program def main() -> unit =
  let w = wrap(Color::Blue)
  case w of
    | wrap(Red) =>
      print "%{Red()}"
    | wrap(Blue) =>
      print "blue"
