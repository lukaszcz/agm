# Library module that consumes the even/odd cycle and exports an enum result.
# Imports `even` (which imports `odd`), so the entry reaches a three-module graph.
import even::*
enum Parity
  | even-p
  | odd-p

def classify(n: int) -> Parity = if is-even(n) => even-p else => odd-p
