# A program def is an ordinary first-class callable, not a host-only entry.
var invoked: bool = false

def invoke(f: () -> unit) -> unit = f()

program def entry() -> unit =
  if
    | invoked => print "called"
    | else =>
      invoked := true
      invoke(entry)
