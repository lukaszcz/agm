# `_` evaluates and discards values without introducing a readable binding.
param initial: int

var effects: int = initial

def produce(label: text) -> text =
  effects := effects + 1
  "discarded ${label}"

let _ = produce("first")
let _ = produce("second")
print effects
