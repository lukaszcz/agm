# break exits the innermost loop.
# Scenarios:
#   - break inside if branch exits immediately
#   - break in an inline body exits immediately
param target: int

program def main() -> unit =
  var n: int = 0
  do
    n := n + 1
    if n >= target => break
  done
  print n
