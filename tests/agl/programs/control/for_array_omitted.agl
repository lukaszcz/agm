# for x in array do body — omitted terminator (dedent triggers synthetic done).
# Exercises: omitted-terminator form of for loop.

program def main(items: array[int]) -> unit =
  var total: int = 0
  for x in items do
    total := total + x
  print total
