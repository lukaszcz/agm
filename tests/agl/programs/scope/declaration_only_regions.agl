scope Block
def hidden() -> int = 0
let helper: Agent = AgentCommand("helper")
end Block

def Shorthand::hidden() -> int = 0
scope Shorthand
let helper: Agent = AgentCommand("helper")
end Shorthand

program def main() -> unit =
  print "scope declarations are deferred"
