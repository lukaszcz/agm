# `_` evaluates and discards values without introducing a readable binding.
# Its annotations are deliberately ignored because it has no binding type.
param initial: int

program def main() -> unit =
  var effects: int = initial
  effects := effects + 1
  let _: text = "discarded first"
  effects := effects + 1
  var _: text = "discarded second"
  print effects
