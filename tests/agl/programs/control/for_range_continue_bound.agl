# Range for + do[n] bound, body always continues: continue counts as a body entry.
# Exercises: continue in range for with safety bound, range exhaustion vs bound.
param n: int
param bound: int

for i in 1 to n do[bound]
  continue
done
print "done"
