# Modules: a re-export facade forwards an unannotated var binding, writable
# bare through the facade's own import tail, exactly like an annotated one.
import facade::*

program def main(delta: int) -> unit =
  total := delta
  print total
  print total-doubled()
