# Explicitly close independent sessions, or leave cleanup to close them on exit.

program def main(explicit_close: bool) -> unit =
  let writer = Session::open(AgentPi("writer", "model", "high"))
  let reviewer = Session::open(AgentPi("reviewer", "model", "high"))
  if explicit_close =>
    writer.close()
    reviewer.close()
