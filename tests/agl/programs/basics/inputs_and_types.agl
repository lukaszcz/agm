# Host inputs: untyped defaults to text; typed inputs are JSON-validated.
param topic
param rounds: int
param ratio: decimal
param flag: bool
param meta: json

print topic
print rounds
print ratio
print flag
print meta
let summary = ask "Summarize ${topic} in ${rounds} rounds (ratio ${ratio}, flag ${flag})."
print summary
