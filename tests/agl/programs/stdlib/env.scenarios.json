{
  "scenarios": [
    {
      "name": "ambient_environment_supports_optional_and_mutating_sugar",
      "params": {"mode": 1},
      "expect": {
        "stdout": "Option::None\n1\ntrue\nOption::None\n",
        "calls": {}
      }
    },
    {
      "name": "extended_creates_an_independent_overlay",
      "params": {"mode": 2},
      "expect": {
        "stdout": "original\noverride\nvalue\nfalse\n",
        "calls": {}
      }
    },
    {
      "name": "extended_merges_over_a_process_environment_snapshot",
      "params": {"mode": 4},
      "process_environment": {
        "base": "original",
        "preserved": "kept"
      },
      "expect": {
        "stdout": "original\noverride\nvalue\nkept\n",
        "calls": {}
      }
    },
    {
      "name": "getenv_raises_the_typed_missing_key_exception",
      "params": {"mode": 3},
      "expect": {
        "stdout": "",
        "raises": {"type": "KeyError", "fields": {"key": "missing"}},
        "calls": {}
      }
    }
  ]
}
