import std/option::Option
import std/pair::Pair
import std/errors::KeyError

extern def dict[text, V]::size(self) -> int
def dict[text, V]::is-empty(self) -> bool = self.size() == 0
extern def dict[text, V]::get(self, key: text) -> V
@extern-name("get_option")
extern def dict[text, V]::get?(self, key: text) -> Option[V]
extern def dict[text, V]::remove(self, key: text) -> V
extern def dict[text, V]::set(self, key: text, value: V) -> unit
@extern-name("remove_option")
extern def dict[text, V]::remove?(self, key: text) -> Option[V]
extern def dict[text, V]::contains(self, key: text) -> bool
extern def dict[text, V]::clear(self) -> unit
extern def dict[text, V]::keys(self) -> array[text]
extern def dict[text, V]::values(self) -> array[V]
extern def dict[text, V]::entries(self) -> array[Pair[text, V]]
extern def dict[text, V]::merge(self, other: dict[text, V]) -> dict[text, V]
@extern-name("merge_in_place")
extern def dict[text, V]::merge!(self, other: dict[text, V]) -> unit
@extern-name("map_values")
extern def dict[text, V]::map-values[U](self, function: (V) -> U) -> dict[text, U]
extern def dict[text, V]::select(self, predicate: (text, V) -> bool) -> dict[text, V]
@extern-name("select_in_place")
extern def dict[text, V]::select!(self, predicate: (text, V) -> bool) -> unit
extern def dict[text, V]::each(self, function: (text, V) -> unit) -> unit

@extern-name("from_entries")
extern def from-entries[V](values: array[Pair[text, V]]) -> dict[text, V]
