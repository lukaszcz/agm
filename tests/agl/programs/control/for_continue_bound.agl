# for x in array do[k] body that always continues:
# continue still advances the iterator and counts toward the bound,
# so MaxIterationsExceeded is raised after k iterations.

program def main(items: array[int], k: int) -> unit =
  for x in items do[k]
    continue
  done
