# Range for with 'to': ascending inclusive iteration with by step.
# Exercises: for i in a to b by k, step overshoot, degenerate (zero-iteration) range.
param start: int
param stop: int
param step: int

var total: int = 0
for i in start to stop by step do
  total := total + i
done
print total
