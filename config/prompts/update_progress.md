Read .agent-files/tasks/TASK_*.md. Update (or create if doesn't exist) .agent-files/tasks/PROGRESS.md which tracks task progress - completed and remaining tasks, next unblocked task. Update and verify based on the actual codebase source.

Commit in .agent-files (git repo separate from main repo) after updating .agent-files/tasks/PROGRESS.md
