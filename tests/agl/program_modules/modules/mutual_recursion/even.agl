# Library module forming a cyclic import with `odd`: `is_even` calls `is_odd`
# from the imported module, which in turn calls back into `is_even`.
import odd::*
def is-even(n: int) -> bool = if n == 0 => true else => is-odd(n - 1)
